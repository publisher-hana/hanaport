import {FONTSTYLE } from '../styles/variables/fontStyle';
import {LabelStyle, colors, contWidth, flexOpt, ellipsis, MouseOver, ScrollStyle, Transition, PosTopCenter, MediaWidth, FullFixed} from '../styles/variables/variables';

export const customStyles = {
    font: {
        size: FONTSTYLE.size,
        weight: FONTSTYLE.weight
    },
    LabelStyle: {
        rdSize: LabelStyle.rdSize,
        border: LabelStyle.border,
        labeBg: LabelStyle.labeBg,
    },

    contWidth: contWidth,
    flexOpt: flexOpt, 
    colors: colors,
    ellipsis:ellipsis,
    MouseOver:MouseOver,
    ScrollStyle:ScrollStyle,
    Transition:Transition,
    PosTopCenter:PosTopCenter,
    MediaWidth:MediaWidth,
    FullFixed:FullFixed
    
};